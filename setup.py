from distutils.core import setup
import setuptools


setup(
    name='pyvirbox',
    version='1.0',
    install_requires=[
    ],
    packages=[
        'pyvirbox'
    ],
    entry_points={
        "console_scripts": [
        ]
    },
   )
